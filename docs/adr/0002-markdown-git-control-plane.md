# Markdown And Git Are The Control Plane

Personal Agent OS will treat Git-backed Markdown as the authoritative Control Plane for policy, workflow state, templates, and approval records. Runtime configuration may cache or parse this information, but the durable source of truth remains the files humans can review and change through Git.
