# Use paos As The Runtime CLI Name

The Runtime command-line and daemon entrypoint will be named `paos`. The short name keeps the local tool convenient while the domain context defines it as the Personal Agent OS Runtime entrypoint, not as a separate generic AgentOS product.
