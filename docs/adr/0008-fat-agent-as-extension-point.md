# Keep Fat Agent As An Extension Point

The Runtime will prefer deterministic Core Tools for high-frequency and policy-sensitive work. Fat Agent behavior for long-tail scripts or third-party APIs is preserved as a future controlled extension point, but it is not a default first-version capability and must remain behind explicit execution, data-source, and approval boundaries.
